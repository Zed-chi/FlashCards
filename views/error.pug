extends layout.pug

block content
	section.desk
		h1 #{error.message} - #{error.status}
		img(src="/static/img/desk.svg" class="desk-items")
		a(class="button-primary-small" href="/") Go Home
		.paper
			pre.stack-trace= error.stack
		.tear

		